// Filter Doctor change
/*let mixMenu = mixitup(".doctor__wrapper", {
    load: {
      filter: ".doctor",
    },
    selectors: {
      target: ".doctors__card",
    },
    animation: {
      duration: 800,
    },
  });
  
  const menuItems = document.querySelectorAll(".doctors__item");
  
  function activeMenuItem() {
    menuItems.forEach((item) => item.classList.remove("doctors__item--active"));
    this.classList.add("doctors__item--active");
  }
  
  menuItems.forEach((item) => item.addEventListener("click", activeMenuItem));
*/
  